// Objeto Math

let resultado;

resultado = Math.PI;
resultado = Math.round(2.5); // redondeo normal
resultado = Math.ceil(2.1); // ceil simpre redondea hacia arriba
resultado = Math.floor(2.9); // floor simpre redondea hacia abajo
resultado = Math.sqrt(144); //raiz cuadrada
resultado = Math.abs(-200); // convierte un numero negativo en positivo
resultado = Math.min(3, 5, 1, 8, 2, 10 ); // nos dice el numero menor de un listado
resultado = Math.max(3, 5, 1, 8, 2, 10 ); // nos dice el numero mayor de un listado
resultado = Math.random(); // genera un número aleatorio menor que 1
resultado = Math.floor( Math.random() * 30); // genera un número aleatorio menor que 1



console.log(resultado);